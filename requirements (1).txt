# Streamlit app runtime
streamlit

# PDF parsing + table extraction / text extraction
pdfplumber
pdfminer.six
Pillow

# Data handling / Excel
pandas
openpyxl

# PDF generation / overlay
reportlab
PyMuPDF

# Optional OCR (requires system packages: tesseract & poppler)
pdf2image
pytesseract

# Helpers (optional)
python-dotenv
